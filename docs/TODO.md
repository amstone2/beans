# TODO

- [ ] Rename `BeanFlavorTag` table to `FlavorNotes` in db schema
- [ ] Talk with Tanner about screen layout — considering a single screen that handles display, filter, add, and delete beans all in one place
- [ ] Evaluate replacing `expect`/`actual` classes (Beta) with an interface-based approach — see `DatabaseDriverFactory` in `commonMain`, `androidMain`, and `iosMain`
- [ ] Write or find an object for date so we can insert it into the database correctly